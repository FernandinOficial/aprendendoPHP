<?php

    $dia = 10;
    $diaAtual = '10';

    /*if($dia == $diaAtual)
    {
        echo 'Igual';
    }
    */
    
    /*if($dia === $diaAtual)
    {
        echo 'Identico';
    }
    */

    // (num1 > 0) && (num2 > 10);  //corresponde ao E
    // (num1 > 0) || (num2 > 10);  //corresponde ao OU

    // ! não
    // != não igual/diferente

    // literal = string
    // real = float ou double
    // inteiro = int ou integer

    // $resposta = $dia < 10 ? 'verdadeiro' : 'falso';

    $var = 1;
    var_dump($var) //indicar ou declarar o tipo da variável
?>